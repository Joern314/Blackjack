module.exports = {
    ignoreFiles: [
        'official/*',
        'updates.json',
        'todo'
    ],
    // Global options:
    verbose: true,
    // Command options:
    build: {
        overwriteDest: true
    }
};